# blenderserver — SaaS Middle-tier

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# LLM integration
anthropic>=0.49.0

# Async HTTP (for calls to blenderworker / external APIs)
httpx>=0.28.0

# Database
aiosqlite>=0.20.0

# Redis queue (optional — install for production)
redis[hiredis]>=5.2.0

# Authentication
pyjwt>=2.9.0

# Metrics / observability
prometheus_client>=0.21.0

# Async database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0
aiosqlite>=0.20.0

# Email validation
email-validator>=2.2.0

# S3-compatible object storage (MinIO, AWS S3)
boto3>=1.35.0

# Stripe billing
stripe>=8.0.0

# Development
pytest>=8.0.0
httpx>=0.28.0  # for TestClient
